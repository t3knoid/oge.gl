# oge.gl Frontend

This directory is reserved for the React frontend described in the repository documentation.

The first implementation slice keeps the frontend scaffold intentionally minimal while the API and scraper contracts are established.
